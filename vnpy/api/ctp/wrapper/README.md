This directory helps to wrap CTP API with Boost.python automatically.

You should execute the scripts like `generate#-*.py` one by one, and the wrapped `.h` and `.cpp` files will be found under the `./final` directory.
